import colors from './Colors';

const styles = {
    page: {
        display: 'flex',
        width: '100vw',
        height: '100vh',
        backgroundColor: colors.mainBackground,
    },
};

export default styles;
